May the odds be ever in your favor
-------------------------------------
Setup:
* Install numpy
* Install nltk
* In Python shell, run:
  * import nltk
  * nltk.download()

Then you can run:
organize.py -d [path to folder with files]
